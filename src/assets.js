/* Fixed brand assets.
   These live in public/ and are intentionally NOT part of business.json —
   the logo and the wrap-paper pattern are the identity and should not be
   changeable from the admin dashboard. To swap one, replace the file in
   public/ (keep the same filename) or edit the path here. */

export const LOGO = '/logo.png';
export const PATTERN = '/pattern.jpg';
