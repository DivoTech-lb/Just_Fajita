/* Fixed brand assets, living in public/.
   LOGO is intentionally NOT part of business.json — it's the identity and
   should not be changeable from the admin dashboard. To swap it, replace
   the file in public/ (keep the same filename) or edit the path here.

   PATTERN is the default/fallback wrap-paper background — still used
   as-is by the offers carousel. The hero section's own background pattern
   is editable via business.json's brand.hero_pattern instead. */

export const LOGO = '/logo.png';
export const PATTERN = '/pattern.jpg';
